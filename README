## changes
- created 4 directories man, lib, skey and include.
- created configure.ac from autoscan.
- created makefile.am for each directories.
- modified configure.ac, and makefile.am.
- copied source files from hw3 into these 4 directories.
- created skeytest.sh in skey for testing using "make check".
- created buildall. It will build, check and then install. 


## buildall
change the SCRDIR in the script to the source files directory
then just run this script. It will build, check and then install.






